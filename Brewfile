# Taps (repos)
tap "homebrew/bundle"
tap "homebrew/cask"
tap "homebrew/core"

# brews (packages)
brew "bat"
brew "exa"
brew "httpie"

# Casks (applications)
cask "alfred"
cask "dropbox"
cask "google-chrome"
cask "visual-studio-code"
